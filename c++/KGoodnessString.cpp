#include <iostream>
#include <vector>

using namespace std;

int main(int argc, char const *argv[])
{
    int t;
    scanf("%d", &t);
    
    int i = 1;

    while (i <= t) {
        int n, k;
        scanf("%d %d", n, k);

        string input;
        scanf("%s", input);

        vector<string> inputv;
        
    }
    return 0;
}
